# 🚀 Инструкция по Деплою (Запуску на сервере)

Эта инструкция поможет вам запустить проект Shark P2P на реальном сервере, чтобы он работал 24/7 и был доступен пользователям.

## 1. Подготовка (Покупки)

### Шаг 1.1: Аренда Сервера (VPS)
Вам нужен сервер на **Ubuntu 22.04**.
*   **Где купить:**
    *   [Aeza](https://aeza.net) (Россия/Европа) — удобно платить картами РФ. Берите локацию **Стокгольм, Хельсинки или Франкфурт**.
    *   [Hetzner](https://hetzner.com) (Европа) — если есть зарубежная карта.
*   **Рекомендуемый тариф:**
    *   Процессор: 2 vCPU
    *   Память: 4 GB RAM
    *   Диск: 40 GB NVMe
    *   Цена: ~600-800 руб/мес.

### Шаг 1.2: Доменное имя
Telegram Mini Apps требуют защищенного соединения (**HTTPS**), поэтому без домена не обойтись.
*   **Где купить:** [Reg.ru](https://reg.ru) или [Namecheap](https://namecheap.com).
*   Купите любой дешевый домен (например, `myshark-app.ru` или `.site`).
*   **Настройка DNS:** В панели регистратора домена создайте **A-запись**:
    *   Host: `@`
    *   Value: `IP-адрес-вашего-сервера` (который выдали при покупке VPS).

### Шаг 1.3: Прокси (Важно!)
Чтобы биржи не забанили сервер за частые запросы.
*   Купите 5-10 штук **IPv4 прокси** (Server Proxy).
*   Формат: `http://user:pass@ip:port`.

---

## 2. Настройка Сервера

### Шаг 2.1: Подключение
Откройте терминал на своем компьютере и подключитесь к серверу:
```bash
ssh root@ваш-ip-сервера
# Введите пароль, который прислал хостинг (при вводе его не видно)
```

### Шаг 2.2: Установка программ
Скопируйте и вставьте эти команды по очереди:

```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем Node.js 20, Git, Nginx и Certbot
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs git nginx certbot python3-certbot-nginx

# Устанавливаем PM2 (менеджер процессов, чтобы сервер не падал)
npm install -g pm2
```

---

## 3. Загрузка и Запуск Проекта

### Шаг 3.1: Скачиваем код
```bash
# Переходим в папку для сайтов
cd /var/www

# Клонируем ваш репозиторий
git clone https://github.com/pipone-one/p2pminiapp.git app

# Заходим внутрь
cd app
```

### Шаг 3.2: Сборка Frontend (Приложения)
Мы должны превратить код React в готовые файлы.
```bash
# Устанавливаем зависимости для всего проекта
npm install

# Собираем приложение
npm run build
```
*После этого появится папка `dist`.*

### Шаг 3.3: Настройка Backend (Сервера)
```bash
cd backend

# Устанавливаем зависимости бэкенда
npm install

# Создаем файл настроек
nano .env
```
В открывшемся редакторе вставьте свои данные:
```env
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен_бота_от_BotFather
# Если хотите починить MEXC, нужен ключ RapidAPI (необязательно)
RAPIDAPI_KEY=ваш_ключ_rapidapi

# Если купили прокси, перечислите их через запятую:
PROXY_LIST=http://user:pass@ip:port,http://user:pass@ip2:port
# Секретный ключ для админки (нужен для входа в панель)
ADMIN_SECRET=admin123
```
*Нажмите `Ctrl+X`, затем `Y`, затем `Enter` для сохранения.*

**Запуск Бэкенда:**
```bash
# Запускаем сервер через PM2
pm2 start server.js --name "shark-backend"

# Сохраняем, чтобы запускался сам после перезагрузки
pm2 save
pm2 startup
```

---

## 4. Настройка Домена и HTTPS (Nginx)

Теперь настроим Nginx, чтобы он принимал запросы на ваш домен и перенаправлял их на наше приложение.

### Шаг 4.1: Настройка конфига
```bash
nano /etc/nginx/sites-available/default
```
Удалите всё содержимое (можно зажать `Ctrl+K`) и вставьте это:

```nginx
server {
    listen 80;
    server_name ваш-домен.ru; # ЗАМЕНИТЕ НА ВАШ ДОМЕН

    location / {
        proxy_pass http://localhost:3000; # Наш Node.js сервер
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```
*Замените `ваш-домен.ru` на реальный домен!*
*Сохраните: `Ctrl+X` -> `Y` -> `Enter`.*

### Шаг 4.2: Проверка и перезапуск
```bash
nginx -t # Должно написать "syntax is ok"
systemctl restart nginx
```

### Шаг 4.3: Получение SSL сертификата (HTTPS)
Это сделает замочек 🔒 в браузере.
```bash
certbot --nginx -d ваш-домен.ru
```
*Вас попросят ввести email (введите любой) и согласиться с правилами (Y).*

---

## 5. Готово!

Теперь ваше приложение доступно по адресу: `https://ваш-домен.ru`

**Полезные команды:**
*   `pm2 status` — проверить статус сервера
*   `pm2 logs` — смотреть логи (ошибки, запросы)
*   `pm2 restart shark-backend` — перезагрузить сервер (если поменяли код)
